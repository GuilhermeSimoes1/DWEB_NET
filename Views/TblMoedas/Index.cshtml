@model IEnumerable<DWEB_NET.Models.TblMoedas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeMoeda)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeMoeda)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MoedaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MoedaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MoedaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
